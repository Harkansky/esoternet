<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Fiche Pacte - {{ entity.name }}</title>
    <style>
        body { font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; background: #fff; margin: 40px; }
        .container { border: 1px solid #ddd; box-shadow: 0 4px 8px rgba(0,0,0,0.1); padding: 20px; border-radius: 10px; background: #fafafa; }
        h1 { color: #2a2a2a; font-size: 24px; font-weight: bold; margin-bottom: 10px; }
        p { font-size: 16px; color: #4a4a4a; line-height: 1.5; }
        h2 { font-size: 20px; font-weight: bold; color: #2a2a2a; margin-top: 20px; margin-bottom: 10px; }
        .feature { margin-bottom: 5px; }
        .value { font-weight: bold; }
    </style>
</head>
<body>
<div class="container">
    <h1>{{ entity.name }}</h1>
    <p class="feature"><strong>Effet :</strong> <span class="value">{{ entity.effect }}</span></p>
    <p class="feature"><strong>Durée :</strong> <span class="value">{{ entity.duration }} jours</span></p>
</div>
</body>
</html>
